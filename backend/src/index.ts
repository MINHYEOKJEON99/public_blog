#!/usr/bin/env node

/**
 * Public Blog API Server
 * Main entry point for the backend application
 */

import 'express-async-errors'
import './server'

// The server will start automatically when server.ts is imported